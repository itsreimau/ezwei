{
  "name": "ezwei",
  "version": "1.0.1",
  "description": "Telegram bot with only eval and exec commands",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "start:pm2": "pm2 start index.js --name $(basename $(pwd))"
  },
  "author": "Muhamad Ikbal Maulana (ItsReimau)",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.2.3",
    "telegraf": "^4.16.3"
  }
}
